# JS201-CW-Project
1. Add all the js files inside Script_file folder
2. Add all the CSS files inside style_file folder
3. Put all the images inside image folder
4. All the html pages should be inside the main folder (JS201-CW-PROJECT)
